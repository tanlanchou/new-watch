export enum socialType {
  weibo = 1,
  douban = 2,
  zhihu = 3,
}

export enum userStatus {
  normal = 1,
  disable = 2,
  delete = 3,
}

export enum userType {
  normal = 1,
  vip = 2,
  spcial = 3,
}

export enum executeStatus {
  unExec = 0,
  success = 1,
  error = 2,
}
